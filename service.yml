apiVersion: v1
kind: Service
metadata:
  name: capstone-website-service
spec:
  type: NodePort
  selector:
    app: capstone-website
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30008
